# Voicegroup LSP

Language support for Pokemon GBA `sound/voicegroups/*.inc` files.

Build an installable VS Code extension with:

```sh
npm install
npm run package
```

Then install the generated `voicegroup-lsp-vscode-darwin-arm64.vsix` with VS Code.
The packaged extension bundles a macOS arm64 C++ server binary.
