## Next Steps
- [ ] Synchronize voices in selected voicegroup between poryaaaa and ccomidi devices using v8 Global. Emitted object can resemble current state.json.

- [ ] Consume richer slot fields (`type`, `supportsEnvelope`, `envelope`) once state.json grows
      them. Use the selected slot to gate Send Env / hint per-slot defaults; no umenu change.
